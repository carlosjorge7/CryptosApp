import { FilterCoinPipe } from './filter-coin.pipe';

describe('FilterCoinPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoinPipe();
    expect(pipe).toBeTruthy();
  });
});
